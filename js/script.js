/**
 * @fileoverview Entry point degli esercizi JS Palindroma e Pari e Dispari.
 *
 * Nota: per eseguire un esercizio, abilita la chiamata corrispondente:
 * `playPalindrome()` o `playEvenOdd()`.
 */

import { playPalindrome } from './palindrome.js';
import { playEvenOdd } from './even-odd.js';

playPalindrome(); // Palindroma
// playEvenOdd(); // Pari e Dispari
